# This directory stores generated ML models from prob_train.py
# Models are ignored by .gitignore but this file keeps the directory structure
# All model files should be saved here for security reasons (path traversal protection)
