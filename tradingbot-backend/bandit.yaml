skips:
  - B110  # try/except pass (best-effort telemetry)
  - B112  # try/except continue (metrics iteration safety)
  - B311  # random for non-crypto backoff
exclude:
  - tests
  - .venv
  - venv
  - build
  - dist
exclude_dirs:
  - tests
  - .venv
  - venv
  - build
  - dist

# Rapportera endast medel och hög allvarlighet
severity: MEDIUM

# Kräv minst MEDIUM confidence (reducerar brus)
confidence: MEDIUM
